services:
  telegram-bots:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram-bots
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
